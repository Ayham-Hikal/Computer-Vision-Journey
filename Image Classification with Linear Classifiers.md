Image classification


1 - image and how computer reads it
image classification is a core task in CV, but what is image classification?
it is the process of assigning a label to an image by the system

an image is defines as a tensor of integers between 0 and 255
how does the system understand the photo, as it doesn't understand anything other than bits (0-1)
the point is that the picture consists of some pixels, where each pixel holds a value between 0 and 255 where 0 is white and 255 is black


2 - the problem of illumination in image classification tasks

3 - the problem of background clutter

4 - the challenge of occlusion

5 - the challenge of intraclass variation

challenge of context


# - )  Data driven approach:

1.)  collect a dataset of images and labels.
2.)  use an ML algorithms to train a classifier.
3.)  evaluate the classifier on new images.
