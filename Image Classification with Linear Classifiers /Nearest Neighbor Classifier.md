the nearest neighbor classifier has 2 main functions:
1.) the train function: it memorizer all data and labels
2.) the predict function: predicts the label of the most similar training image

the distance matrix takes the input photo and compares it with each image in the dataset and then returns a value which defines the similarity between these two inputs.

1 way of doing that is the follow: <img width="1060" height="597" alt="Screenshot 2026-02-23 at 01 02 18" src="https://github.com/user-attachments/assets/01603329-8b7b-4ee9-a741-2854613bd246" />

